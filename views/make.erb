<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>パズルを作る</title>
</head>
<body>
  <div class="top">
    <form action="/new_puzzle" method="post" enctype="multipart/form-data">
      <P><input type="text" name="name"></P>
      <input type="submit" value="POST">
    </form>
    <% @puzzles.each do |puzzle| %>
      <div class="make">
        <p><%= puzzle.name %></p>
          <div class="make_box">
            <div class="make_container">
              <form action="/delete_puzzle/<%= puzzle.id %>" method="post">
                <input type="submit" value="DELTE_ALL">
              </form>
              <form action="/new/<%= puzzle.id %>" method="post" enctype="multipart/form-data">
                <P><input type="file" name="file"></P>
                <input type="submit" value="POST">
              </form>
            </div>
            <div class="images">
              <% puzzle.images.each do |image| %>
                <div class="image">
                  <form action="/delete_image/<%= image.id %>" method="post">
                    <input type="submit" value="DELTE">
                  </form>
                  <% unless image.img.empty? %>
                    <p><img src="<%= image.img %>"></P>
                  <% end %>
                </div>
              <% end %>
            </div>
          </div>
      </div>
    <% end %>
  </div>
</body>
</html>